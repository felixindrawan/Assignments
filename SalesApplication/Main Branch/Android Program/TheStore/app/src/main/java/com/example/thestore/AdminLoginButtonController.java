package com.example.thestore;

import android.content.Context;
import android.content.Intent;
import android.view.View;
import android.widget.EditText;
import android.widget.TextView;
import androidx.appcompat.app.AppCompatActivity;

public class AdminLoginButtonController implements View.OnClickListener{
 /* private Context appContext;

  public AdminLoginButtonController(Context context){
    this.appContext = context;
  }*/

  @Override
  public void onClick(View view){
    /*Intent intent = new Intent(this.appContext, AdminActivity.class);

    TextView welcomeTV = (TextView) ((AppCompatActivity)appContext).findViewById(R.id.welcome_title);
    TextView loginTV = (TextView) ((AppCompatActivity)appContext).findViewById(R.id.login_title);
    EditText usernameET = (EditText) ((AppCompatActivity)appContext).findViewById(R.id.username_title);
    EditText passwordET = (EditText) ((AppCompatActivity)appContext).findViewById(R.id.password_title);

    ((AppCompatActivity)appContext).startActivity(intent);*/
  }
}
